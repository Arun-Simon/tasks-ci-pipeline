name: artifact
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello" > file.txt
      - uses: actions/upload-artifact@v4
        with:
          name: app
          path: file.txt
